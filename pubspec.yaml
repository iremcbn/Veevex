name: veevex
description: Elektrikli araçlar için şarj istasyonu uygulaması
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.25.4
  firebase_auth: ^4.17.4
  google_sign_in: ^6.2.1
  google_maps_flutter: ^2.5.0
  provider: ^6.1.2
  cloud_firestore: 4.14.1
  geolocator: ^11.0.0 
  firebase_storage: ^11.6.6
  image_picker: ^1.0.7
  intl: ^0.19.0
  flutter_map: ^6.2.1
  latlong2: ^0.9.1
  http: ^0.13.5
  webview_flutter: ^4.0.7
  

  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true